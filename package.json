{
  "name": "nextjs-chrome-extension",
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=extension-test-app",
    "build": "npm run build --workspaces --if-present",
    "build:extension": "npm run build --workspace=app-ui && npm run build-extension:ui --workspace=utility-scripts && npm run build:extension-specific",
    "build:extension-specific": "npm run build --workspace=extension-specific && npm run build-extension:specific --workspace=utility-scripts"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "prettier-plugin-tailwindcss": "^0.5.4"
  }
}
