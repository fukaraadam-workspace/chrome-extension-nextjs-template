{
  "name": "nextjs-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:extension": "npm run build --workspace=app-ui --workspace=extension-specific && npm run build-extension:ui --workspace=utility-scripts && npm run build-extension:specific --workspace=utility-scripts"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "prettier-plugin-tailwindcss": "^0.5.4"
  }
}
