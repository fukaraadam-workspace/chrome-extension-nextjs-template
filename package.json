{
  "name": "nextjs-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "prettier-plugin-tailwindcss": "^0.5.4"
  }
}
